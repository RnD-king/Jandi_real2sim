schema_version: 3

campaign:
  id: null
  output_root: data/raw/mode5
  processed_root: data/processed/mode5
  results_root: results

components:
  hardware: hardware.yaml
  controller: controller.yaml
  safety: safety.yaml
  bench_geometry: bench/geometry.yaml
  bench_loads: bench/loads.yaml
  trajectories:
    accelerated_oscillation: trajectories/accelerated_oscillation.yaml
    slow_plus_highfreq: trajectories/slow_plus_highfreq.yaml
    slowly_raise_lower: trajectories/slowly_raise_lower.yaml
    static_calibration: trajectories/static_calibration.yaml
    delay_probe: trajectories/delay_probe.yaml

mechanical_configurations:
  - {id: L1_m250, arm_length: L1, load: m250}
  - {id: L1_m500, arm_length: L1, load: m500}
  - {id: L1_m750, arm_length: L1, load: m750}
  - {id: L2_m250, arm_length: L2, load: m250}
  - {id: L2_m500, arm_length: L2, load: m500}
  - {id: L2_m750, arm_length: L2, load: m750}

main_trajectories:
  - accelerated_oscillation
  - slow_plus_highfreq
  - slowly_raise_lower
repetitions: [1, 2, 3]
approach_directions: [approach_positive, approach_negative]

# 반드시 데이터 수집 전에 고정한다. repeat 3은 validation이 아니다.
holdout_configuration: null
execution_order: null
randomization_seed: null
