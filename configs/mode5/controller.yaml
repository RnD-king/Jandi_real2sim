schema_version: 3

# 실제 Jandi에서 사용할 값을 그대로 기록한다. 식별기가 이 값을 바꾸지 않는다.
# P/D, Goal Current/Current Limit, Goal PWM/PWM Limit은 추측값을 금지한다.
mode5_registers:
  operating_mode: 5
  drive_mode: null
  position_p_gain: null
  position_i_gain: 0
  position_d_gain: null
  feedforward_1st_gain: 0
  feedforward_2nd_gain: 0
  profile_velocity: 0
  profile_acceleration: 0
  # MX-106R(2.0) addr 98, 20 ms/count, valid enabled range 1..127.
  bus_watchdog_raw: null
  goal_current_raw: null
  expected_current_limit_raw: null
  goal_pwm_raw: null
  expected_pwm_limit_raw: null
